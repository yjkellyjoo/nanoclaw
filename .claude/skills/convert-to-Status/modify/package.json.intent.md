# Intent: Modify `package.json`

Add Status transport dependencies needed for WebSocket signal handling.

- Add runtime dependency: `ws`.
- Add development dependency: `@types/ws`.
- No other package metadata or script changes are required.
